package Heranca;

public class CalculoCientifico {

    double valor;

    public void raizQuadrada(double a ){

        double raizQuadrada = Math.sqrt(a);
    }

    public void exponienciacao(double a, double b ){
        double exponenciacao = Math.pow(a,b);
    }
}
